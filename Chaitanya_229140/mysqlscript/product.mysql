create database productinfo;
use productinfo;

create table product(
                  title varchar(25),
                  description varchar(25),
                  unitprice double ,
                  balance double
);

insert into product values(" flower","nice",200,5000);
